import React, { useState, useEffect } from 'react';
import { Edit, Heart, Star, Users, Award, Truck } from 'lucide-react';

interface AboutContent {
  heroTitle: string;
  heroSubtitle: string;
  heroImage: string;
  story: string;
  mission: string;
  vision: string;
  values: string[];
  features: Array<{
    icon: string;
    title: string;
    description: string;
  }>;
  team: Array<{
    name: string;
    role: string;
    image: string;
    bio: string;
  }>;
  stats: Array<{
    number: string;
    label: string;
  }>;
}

const About: React.FC = () => {
  const [content, setContent] = useState<AboutContent | null>(null);
  const [loading, setLoading] = useState(true);
  const [isEditing, setIsEditing] = useState(false);

  // Mock content - this would be fetched from API and editable by admin
  useEffect(() => {
    const mockContent: AboutContent = {
      heroTitle: "Fashion That Tells Your Story",
      heroSubtitle: "At DreamFit, we believe every woman deserves to feel confident and beautiful. Our carefully curated collection brings you the latest trends and timeless classics.",
      heroImage: "/api/placeholder/1200/600",
      story: "Founded in 2020 with a simple dream - to make beautiful, high-quality dresses accessible to every woman. What started as a small boutique has grown into a trusted destination for fashion-forward women who value style, quality, and affordability. Our journey began when our founder realized the gap in the market for dresses that combine contemporary design with traditional craftsmanship.",
      mission: "To empower women through fashion by providing high-quality, stylish dresses that make every woman feel confident and beautiful, regardless of the occasion.",
      vision: "To become the leading online destination for women's dresses, known for our exceptional quality, innovative designs, and commitment to customer satisfaction.",
      values: [
        "Quality Craftsmanship",
        "Customer Satisfaction",
        "Inclusive Fashion",
        "Sustainable Practices",
        "Innovation in Design",
        "Community Building"
      ],
      features: [
        {
          icon: "heart",
          title: "Passionate Design",
          description: "Every dress is designed with love and attention to detail, ensuring you look and feel amazing."
        },
        {
          icon: "star",
          title: "Premium Quality",
          description: "We use only the finest materials and work with skilled artisans to create dresses that last."
        },
        {
          icon: "users",
          title: "Customer-Centric",
          description: "Your satisfaction is our priority. We listen to feedback and continuously improve our offerings."
        },
        {
          icon: "award",
          title: "Award-Winning Service",
          description: "Recognized for excellence in customer service and product quality by industry experts."
        },
        {
          icon: "truck",
          title: "Fast Delivery",
          description: "Quick and reliable shipping to get your perfect dress to you as soon as possible."
        }
      ],
      team: [
        {
          name: "Priya Sharma",
          role: "Founder & Creative Director",
          image: "/api/placeholder/300/300",
          bio: "With over 10 years in fashion design, Priya brings creativity and vision to every collection."
        },
        {
          name: "Anita Patel",
          role: "Head of Operations",
          image: "/api/placeholder/300/301",
          bio: "Anita ensures smooth operations and maintains our high standards of quality and service."
        },
        {
          name: "Kavya Reddy",
          role: "Customer Experience Manager",
          image: "/api/placeholder/300/302",
          bio: "Kavya is dedicated to making sure every customer has an exceptional shopping experience."
        }
      ],
      stats: [
        { number: "10,000+", label: "Happy Customers" },
        { number: "500+", label: "Unique Designs" },
        { number: "50+", label: "Cities Served" },
        { number: "99%", label: "Customer Satisfaction" }
      ]
    };

    setTimeout(() => {
      setContent(mockContent);
      setLoading(false);
    }, 500);
  }, []);

  const getIcon = (iconName: string) => {
    switch (iconName) {
      case 'heart': return <Heart className="h-8 w-8" />;
      case 'star': return <Star className="h-8 w-8" />;
      case 'users': return <Users className="h-8 w-8" />;
      case 'award': return <Award className="h-8 w-8" />;
      case 'truck': return <Truck className="h-8 w-8" />;
      default: return <Heart className="h-8 w-8" />;
    }
  };

  if (loading || !content) {
    return (
      <div className="min-h-screen bg-gray-50 animate-pulse">
        <div className="h-96 bg-gray-200"></div>
        <div className="max-w-7xl mx-auto px-4 py-16">
          <div className="space-y-8">
            {[...Array(6)].map((_, index) => (
              <div key={index} className="h-32 bg-gray-200 rounded-lg"></div>
            ))}
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-white">
      {/* Admin Edit Button */}
      <div className="fixed top-20 right-4 z-50">
        <button
          onClick={() => setIsEditing(!isEditing)}
          className={`p-3 rounded-full shadow-lg transition-colors duration-200 ${
            isEditing
              ? 'bg-red-500 text-white hover:bg-red-600'
              : 'bg-white text-gray-600 hover:bg-gray-50'
          }`}
          title={isEditing ? 'Stop Editing' : 'Edit Page'}
        >
          <Edit className="h-5 w-5" />
        </button>
      </div>

      {/* Hero Section */}
      <section className="relative h-96 flex items-center justify-center overflow-hidden">
        <div
          className="absolute inset-0 bg-cover bg-center"
          style={{ backgroundImage: `url(${content.heroImage})` }}
        ></div>
        <div className="absolute inset-0 bg-black/50"></div>
        <div className="relative z-10 text-center text-white max-w-4xl mx-auto px-4">
          <h1 className={`text-5xl md:text-6xl font-bold mb-6 ${isEditing ? 'border-2 border-yellow-400 p-2' : ''}`}>
            {content.heroTitle}
          </h1>
          <p className={`text-xl md:text-2xl ${isEditing ? 'border-2 border-yellow-400 p-2' : ''}`}>
            {content.heroSubtitle}
          </p>
        </div>
      </section>

      {/* Our Story Section */}
      <section className="py-16 bg-white">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">Our Story</h2>
            <p className={`text-lg text-gray-600 leading-relaxed ${isEditing ? 'border-2 border-yellow-400 p-4' : ''}`}>
              {content.story}
            </p>
          </div>
        </div>
      </section>

      {/* Mission & Vision */}
      <section className="py-16 bg-gray-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-12">
            {/* Mission */}
            <div className="bg-white rounded-lg p-8 shadow-sm">
              <h3 className="text-2xl font-bold text-gray-900 mb-4">Our Mission</h3>
              <p className={`text-gray-600 leading-relaxed ${isEditing ? 'border-2 border-yellow-400 p-4' : ''}`}>
                {content.mission}
              </p>
            </div>

            {/* Vision */}
            <div className="bg-white rounded-lg p-8 shadow-sm">
              <h3 className="text-2xl font-bold text-gray-900 mb-4">Our Vision</h3>
              <p className={`text-gray-600 leading-relaxed ${isEditing ? 'border-2 border-yellow-400 p-4' : ''}`}>
                {content.vision}
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Values */}
      <section className="py-16 bg-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">Our Values</h2>
          </div>
          <div className={`grid grid-cols-2 md:grid-cols-3 gap-6 ${isEditing ? 'border-2 border-yellow-400 p-4' : ''}`}>
            {content.values.map((value, index) => (
              <div key={index} className="text-center p-6 bg-gray-50 rounded-lg">
                <div className="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Heart className="h-6 w-6 text-primary-600" />
                </div>
                <h4 className="font-semibold text-gray-900">{value}</h4>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Features */}
      <section className="py-16 bg-gray-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">Why Choose DreamFit?</h2>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {content.features.map((feature, index) => (
              <div key={index} className={`bg-white p-8 rounded-lg shadow-sm text-center ${isEditing ? 'border-2 border-yellow-400' : ''}`}>
                <div className="inline-flex items-center justify-center w-16 h-16 bg-primary-100 text-primary-600 rounded-full mb-6">
                  {getIcon(feature.icon)}
                </div>
                <h3 className="text-xl font-semibold text-gray-900 mb-4">{feature.title}</h3>
                <p className="text-gray-600">{feature.description}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Team */}
      <section className="py-16 bg-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-12">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">Meet Our Team</h2>
            <p className="text-xl text-gray-600">The passionate people behind DreamFit</p>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {content.team.map((member, index) => (
              <div key={index} className={`text-center ${isEditing ? 'border-2 border-yellow-400 p-4' : ''}`}>
                <img
                  src={member.image}
                  alt={member.name}
                  className="w-48 h-48 rounded-full mx-auto mb-6 object-cover"
                />
                <h3 className="text-xl font-semibold text-gray-900 mb-2">{member.name}</h3>
                <p className="text-primary-600 font-medium mb-4">{member.role}</p>
                <p className="text-gray-600">{member.bio}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Statistics */}
      <section className="py-16 bg-primary-600">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className={`grid grid-cols-2 md:grid-cols-4 gap-8 ${isEditing ? 'border-2 border-yellow-400 p-4' : ''}`}>
            {content.stats.map((stat, index) => (
              <div key={index} className="text-center text-white">
                <div className="text-4xl md:text-5xl font-bold mb-2">{stat.number}</div>
                <div className="text-primary-100">{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Call to Action */}
      <section className="py-16 bg-white">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
            Ready to Find Your Perfect Dress?
          </h2>
          <p className="text-xl text-gray-600 mb-8">
            Explore our collection and discover dresses that make you feel amazing
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <button className="px-8 py-3 bg-primary-600 text-white rounded-lg hover:bg-primary-700 transition-colors duration-200 font-medium">
              Shop Collection
            </button>
            <button className="px-8 py-3 bg-white text-primary-600 border-2 border-primary-600 rounded-lg hover:bg-primary-50 transition-colors duration-200 font-medium">
              Contact Us
            </button>
          </div>
        </div>
      </section>

      {/* Edit Mode Notification */}
      {isEditing && (
        <div className="fixed bottom-4 left-4 bg-yellow-500 text-white px-6 py-3 rounded-lg shadow-lg">
          <p className="font-medium">✏️ Edit Mode Active</p>
          <p className="text-sm">Click on highlighted sections to edit content</p>
        </div>
      )}
    </div>
  );
};

export default About;